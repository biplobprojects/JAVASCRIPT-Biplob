//TO CHECK IF AN ARRAY IS EMPTY OR NOT:
// let a = [45];
// console.log(a.length==0 ? true : false);

//TO CHECK IF AN OBJECT IS EMPTY OR NOT:
let Obj = {};
 //to check for empty object:
// if(Object.entries(Obj).length === 0) {
// console.log("object is empty");}
console.log(Object.entries(Obj).length == 0 ? true : false);

//SECOND METHOD:
// console.log(Object.keys(Obj).length == 0 ? 'true' : 'false');